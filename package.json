{
  "name": "root",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "watch": "lerna run watch",
    "build": "lerna run build",
    "start": "lerna --scope @webcave/example-client run dev",
    "prettier": "pretty-quick",
    "lint": "pretty-quick --check"
  },
  "devDependencies": {
    "lerna": "^8.1.3",
    "prettier": "^3.2.5",
    "pretty-quick": "^4.0.0"
  }
}
