import styled from '@emotion/styled'

const ChatBox = styled.div`
`

const ChatBoxText = styled.span`
`

const ChatBoxEntry = styled.input`
`

const Nickname = styled.div`
`

const NicknameEntry = styled.span`
`